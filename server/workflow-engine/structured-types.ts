/**
 * TYPES STRUCTURÉS — RÉEXPORTATION
 * Ce fichier réexporte les types structurés définis dans types.ts
 * pour maintenir la compatibilité avec les imports existants.
 */

export type {
  ProspectData,
  CallData,
  AIData,
  WorkflowVariables,
  EventMetadata,
  StructuredIncomingEvent,
  FinalExecutionContext,
} from './types';
